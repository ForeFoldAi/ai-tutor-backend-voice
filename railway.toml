[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Nest serves health at /rtc/health (root returns 404).
healthcheckPath = "/rtc/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
